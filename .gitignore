# Logs
logs/
*.log
*.out
*.err

# Large data files. Everything here is regenerable by rerunning the pipeline,
# with one exception: the gold set carries manual labels that cannot be
# reproduced from code, so it is force-added and tracked despite these rules.
Processed Data/
raw_data/
*.csv
*.tif
*.zip
*.rds

# Cache
step05_gbif_cache/
step05_env_data/
.cache/
__pycache__/
*.py[cod]

# Local tooling (recreate with: python -m venv .venv_figs)
.venv_figs/
.venv_pptx/
R_library/
